spring:
  application:
    name: finance-guide-backend

  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info

  health:
    redis:
      enabled: false